fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0
sqlalchemy==2.0.36
# Required by SQLAlchemy's asyncio engine (implicit dependency; pinned
# explicitly so a fresh `pip install -r` outside Docker works).
greenlet==3.5.5
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
argon2-cffi==23.1.0
pyjwt==2.10.1
python-multipart==0.0.20
# Passkeys (WebAuthn/Face ID/Touch ID login) — additive to the password
# flow in app/auth, not a replacement. See app/webauthn/.
webauthn==2.7.1
# Laboratory report extraction: digital-PDF text + word-box geometry
# (pure Python, pdfminer.six) and OCR for scanned/image reports.
# pytesseract is a thin wrapper over the system `tesseract` binary — the
# pipeline degrades safely (flags the report, never fabricates a value)
# when the binary is absent. See backend/Dockerfile for the runtime install.
pdfplumber==0.11.4
pytesseract==0.3.13
pillow==11.0.0
redis==5.2.1
celery==5.4.0
minio==7.2.12
python-json-logger==2.0.7
tenacity==9.0.0
httpx==0.28.1
pypdf==5.1.0
qrcode[pil]==8.0
python-barcode==0.15.1
# Server-side PDF generation for the async patient-summary report job
# (app/reports/job_generators.py). Pure Python, no system libraries — the
# Celery worker builds the PDF; the browser never does.
fpdf2==2.8.2
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
factory-boy==3.3.1
